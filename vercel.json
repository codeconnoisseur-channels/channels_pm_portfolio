{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/portfolio build",
  "outputDirectory": "artifacts/portfolio/dist/public",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}

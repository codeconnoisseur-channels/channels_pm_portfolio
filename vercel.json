{
  "installCommand": "npm install --prefix artifacts/portfolio",
  "buildCommand": "npm run build --prefix artifacts/portfolio",
  "outputDirectory": "artifacts/portfolio/dist/public",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
